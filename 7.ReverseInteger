class Solution {
    public int reverse(int x) {
        int copy=x;
        double rev=0;
        while(x!=0)
{
    int d=x%10;
    x/=10;
    rev=rev*10+d;
}   

if(rev<-Math.pow(2,31)||rev>Math.pow(2,31)-1)
rev=0;
int p=(int)rev;

return p;

 }
}
